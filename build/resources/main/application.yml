spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vaultdb?useSSL=false&serverTimezone=UTC
    username: vault
    password: vaultpass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
